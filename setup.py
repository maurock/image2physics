from setuptools import setup, find_packages

setup(name='phys',
      version='0.1',
      description='Learning physics properties from multi view data',
      license='MIT',
      packages=find_packages(),
      zip_safe=False)